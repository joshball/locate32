TITLE=Copy command (Edit menu)
CONTENT=
<div id="menucommands">
<H1><A NAME="hid_edit_copy"></A>Copy command (Edit menu)</H1>

<P>Use this command to copy select files to clipboard. The selected files copied when 
file operation is executed (e.g., by using Windows Explorer). 

This command is unavailable if no files are selected.</P>

<P>Copying data to the clipboard replaces the contents previously stored there.</P>

</div>